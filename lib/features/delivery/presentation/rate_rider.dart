import 'package:flutter/material.dart';

class RateRiderScreen extends StatelessWidget {
  const RateRiderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Rate Rider Screen"),), body: SafeArea(child: Column(children: [],)),);
  }
}