import 'package:flutter/material.dart';

class DetailReceipt extends StatelessWidget {
  const DetailReceipt({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Detail Receipt"),), body: SafeArea(child: Column(children: [],)),);
  }
}