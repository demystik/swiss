import 'package:flutter/material.dart';

class ViewDetailReceiptScreen extends StatelessWidget {
  const ViewDetailReceiptScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("View detail receipt"),), body: SafeArea(child: Column(children: [],)),);
  }
}